# Web framework
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0

# Cấu hình & validate
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
email-validator>=2.0.0,<3.0.0

# Auth (đăng ký/đăng nhập Sale)
bcrypt>=4.1.0,<5.0.0
PyJWT>=2.8.0,<3.0.0

# HTTP client
httpx>=0.27.0,<1.0.0

# Persistence layer (Sprint 1.1) — Postgres + migrations.
# psycopg2-binary: driver Postgres; SQLAlchemy: ORM; alembic: migrations.
# Tất cả tuỳ chọn lúc runtime: thiếu DATABASE_URL → tự fallback JSON.
sqlalchemy>=2.0.0,<3.0.0
psycopg2-binary>=2.9.9,<3.0.0
alembic>=1.13.0,<2.0.0

# Backup DB → Cloudflare R2 (S3-compatible) — scripts/backup_db.py.
boto3>=1.34.0,<2.0.0

# LLM provider (sẽ dùng từ giai đoạn 1)
anthropic>=0.40.0,<1.0.0

# Import CSV/Excel khách hàng (scripts/import_customers.py)
pandas>=2.0.0,<3.0.0
openpyxl>=3.1.0,<4.0.0
requests>=2.31.0,<3.0.0

# Ingest tài liệu dự án (scripts/ingest_projects.py) + retrieval BM25 offline
pypdf>=4.0.0,<6.0.0
pdfplumber>=0.10.0,<1.0.0
python-docx>=1.1.0,<2.0.0
rank-bm25>=0.2.2,<1.0.0

# Sale Learning Center: upload tài liệu (multipart) + sinh phiếu báo giá PDF
python-multipart>=0.0.9,<1.0.0
reportlab>=4.0.0,<5.0.0

# Sales Crew (CrewAI multi-agent "đội sale ảo") — TÍNH NĂNG CỘNG THÊM, mặc định TẮT.
# Deps NẶNG + có thể xung đột version → KHÔNG cài ở build chính. Khai báo riêng
# trong requirements-crew.txt; chỉ cài khi bật crew (CREW_ENABLED=true). Code tự
# fallback heuristic khi crewai chưa được cài (app vẫn boot bình thường).
#   pip install -r requirements-crew.txt   # Python 3.10–3.13 (Railway python311 OK)

# Test (TestClient của Starlette dùng httpx — đã khai báo ở phần HTTP client)
pytest>=8.0.0,<9.0.0
